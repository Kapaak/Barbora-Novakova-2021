//libs
import styled from "styled-components";

export const Line = () => {
	return <StyledLine />;
};

const StyledLine = styled.div`
	background-color: var(--col3);
	width: 100%;
	height: 0.3rem;
	margin: 0.5rem 0;
`;
